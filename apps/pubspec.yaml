name: jmox_app
description: "Junior Mathematics Olympiad (JMO) Management System — Unified Cross-Platform Application (Web, Android, iOS)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  go_router: ^18.0.1
  provider: ^6.1.1
  http: ^1.2.0
  intl: ^0.20.3
  flutter_secure_storage: ^11.2.0
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  camera: ^0.12.1
  image_picker: ^1.0.4
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
